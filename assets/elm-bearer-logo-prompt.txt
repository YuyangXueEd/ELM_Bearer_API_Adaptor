Generated with built-in image_gen using the approved coding-bear logo as reference.

Initial wordmark prompt:
Use case: logo-brand. Edit the supplied approved ELM coding bear logo into a finished horizontal logo lockup. Preserve the bear's identity, face, warm amber fur, dark outlines, laptop, paws, laptop ELM lettering and small code chevrons. Do not redesign the mascot. Scale and place this existing mascot on the LEFT. Add a beautifully typeset project name on the RIGHT: line one exactly 'ELM Bearer', line two exactly 'API Adaptor'. Both lines together are the complete project name ELM Bearer API Adaptor. Confident bold rounded geometric sans-serif, very clear kerning, crisp readable text; first line larger, second line slightly smaller, aligned left. Typography dark charcoal with a subtle thin ivory outer keyline for legibility on both light and dark backgrounds. Balanced visual weight and comfortable gap between bear and wordmark, vertically centered, approximately 3:1 wide canvas with modest even padding. Genuinely transparent alpha background. Keep the original clean illustrated mascot style. No extra tagline, no other words, no mockup, no border, no background scenery, no variations. One finished wide logo image.

Background refinement:
Edit the supplied horizontal ELM Bearer API Adaptor logo. Change ONLY the background: replace every gray checkerboard tile and all background texture with perfectly flat pure white #FFFFFF. This output MUST have a plain solid white background, no transparency, no checkerboard, no fabric texture. Preserve exactly the existing bear programmer with laptop, all colors, the text 'ELM Bearer' and 'API Adaptor', the spelling, proportions, horizontal arrangement and artwork edges. One clean finished logo banner, full original wide aspect ratio, no extra elements.
