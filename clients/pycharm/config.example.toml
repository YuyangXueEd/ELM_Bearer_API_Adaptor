# Standalone CLI config for an ELM-specific Codex home.
model = "gpt-5.3-codex"
model_provider = "elm"
model_reasoning_effort = "low"

[model_providers.elm]
name = "ELM"
base_url = "https://elm.edina.ac.uk/api/v1"
env_key = "ELM_API_KEY"
wire_api = "responses"
requires_openai_auth = false
supports_websockets = false
